services:
  api:
    build: ./API
    ports: 
      - 8080:8080
    volumes:
      - ./API/src:/var/www/html
  gui:
    build: ./gui
    ports:
      - 3000:3000
    volumes:
      - ./gui:/app
      - /app/node_modules
